//Arrays

// What?
// array mtlb  jb bhi apko ek ya ek se jada value ek sath rakhni ho

//var users = ["shivansh", "shiv", "ansh", "Vansh"];
//console.log(users[3]);

//How?

//var arr = [1, 2, "3", function () {}, [], 4];

//why

// a lot of times the data is in the shape of more than member, to contain all data altogether, we can save it in an array

//LOOP WITH ARRAY

var arr = [1, 2, 3, 4, 5];

//how to loop an array

arr.forEach(function (val) {
  console.log(val + 1);
});

//how to loop an array and do something with each element
